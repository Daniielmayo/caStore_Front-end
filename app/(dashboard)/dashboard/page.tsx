"use client";

import React from "react";

export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4" style={{ color: "var(--color-dark)" }}>
        Dashboard
      </h1>
      <p style={{ color: "var(--color-mid)" }}>
        Selecciona una opción del menú de la izquierda.
      </p>
    </div>
  );
}
